<Page x:Class="shop.Views.Admin.OrdersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:shop.Views.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="OrdersView">

    <Grid Background="White">
        <DataGrid x:Name="ProductsGrid" AutoGenerateColumns="False"  IsReadOnly="True">
            <DataGrid.Columns >
                <DataGridTextColumn Header="ID" Width="*" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="USER" Width="*" Binding="{Binding USER_ID}"/>
                <DataGridTextColumn Header="PRODUCT" Width="*" Binding="{Binding PRODUCT_ID}"/>
                <DataGridTextColumn Header="COUNT" Width="*" Binding="{Binding COUNT}"/>
                <DataGridTextColumn Header="DATE" Width="*" Binding="{Binding DATE}"/>
                <DataGridTextColumn Header="ORDER_STATE" Width="*" Binding="{Binding ORDER_STATE}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
