<Page x:Class="shop.Views.AuthView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:shop.Views" xmlns:viewmodels="clr-namespace:shop.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AuthViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthView">

    <Grid Background="White">
        <StackPanel Margin="15, 15,0 ,0" Orientation="Vertical">
            <StackPanel  Orientation="Horizontal">
                <TextBlock FontSize="15" Text="login"/>
                <TextBox x:Name="login" Width="217" />
            </StackPanel>
            <StackPanel  Orientation="Horizontal">
                <TextBlock FontSize="15" Text="password"/>
                <TextBox x:Name="password" Width="217" RenderTransformOrigin="0.5,0.5"/>
            </StackPanel>

            <Button Content="войти через клик" Click="Button_Click" Margin="10"/>
            <!--<Button Content="войти через команду" Command="{Binding LoginCommand}" Margin="10"/>-->



        </StackPanel>
    </Grid>
</Page>
